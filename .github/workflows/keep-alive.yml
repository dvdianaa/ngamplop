name: Keep Supabase Awake

on:
  schedule:
    - cron: '0 3 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -sf "https://sptldzcamdrdfqayhruf.supabase.co/rest/v1/guests_public?select=id&limit=1" \
            -H "apikey: sb_publishable_DBdbG9reb1CBYeaW4jElXw_FxEoOqoF" \
            -H "Authorization: Bearer sb_publishable_DBdbG9reb1CBYeaW4jElXw_FxEoOqoF" \
            -o /dev/null -w "HTTP %{http_code}\n"
